import 'package:flutter/material.dart';

class AppColors {
  // Light Theme Colors
  static const Color primaryLight = Color(0xFF2196F3);
  static const Color secondaryLight = Color(0xFFE3F2FD);
  static const Color onErrorLight = Color(0xFFF44336);

  // Dark Theme Colors
  static const Color primaryDark = Color(0xFF64B5F6);
  static const Color secondaryDark = Color(0xFF37474F);
  static const Color onErrorDark = Color(0xFFE57373);

  // Neutral Colors (usable in both themes)
  static const Color backgroundLight = Colors.white;
  static const Color backgroundDark = Color(0xFF303030);
  static const Color textLight = Colors.black;
  static const Color textDark = Colors.white;
}
